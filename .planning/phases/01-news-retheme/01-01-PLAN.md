---
phase: 01-news-retheme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/News.tsx
autonomous: true
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03
  - NEWS-04
  - NEWS-05
  - NEWS-06
  - NEWS-07
  - NEWS-08
user_setup: []

must_haves:
  truths:
    - "News screen has light background matching other app tabs (#fcfcfc)"
    - "Story cards use white-on-light tonal layering per DESIGN.md"
    - "Typography uses ClashDisplay-Bold, CabinetGrotesk, Inter — same as Dashboard"
    - "HN orange (#ff6600) and YC branding are removed"
    - "PageHeader uses default (non-dark) mode matching other tabs"
    - "Detail overlay uses app's DetailOverlay component with restyled content"
    - "Vertical paging interaction still works (fetching, pagination, detail view)"
  artifacts:
    - path: "src/News.tsx"
      provides: "Re-themed news screen with 'Curated Journal' design"
      min_lines: 280
      contains:
        - "backgroundColor: '#fcfcfc'"
        - "fontFamily: 'ClashDisplay-Bold'"
        - "fontFamily: 'Inter-Regular'"
        - "fontFamily: 'CabinetGrotesk-Bold'"
        - "<PageHeader title="
  key_links:
    - from: "src/News.tsx"
      to: "DESIGN.md"
      via: "Design token values (#fcfcfc, #1a1c1c, #474747, tonal surfaces)"
      pattern: "fcfcfc|1a1c1c|#ffffff"
    - from: "src/News.tsx"
      to: "src/Heroicon.tsx"
      via: "Icon import and usage"
      pattern: "import.*Heroicon"
    - from: "src/News.tsx"
      to: "src/components/PageHeader.tsx"
      via: "PageHeader component without dark prop"
      pattern: "PageHeader title="
---

<objective>
Re-theme the News screen to use the app's "Curated Journal" design system while preserving all Hacker News API integration and vertical-paging UX.

**Purpose:** The News tab currently uses a dark Hacker News theme (#0d0d0d bg, #ff6600 orange accents) that is visually inconsistent with the rest of the app's light, monochrome "Curated Journal" aesthetic. This plan applies the same design tokens and component patterns used by Dashboard, Hackathons, and Courses tabs.

**Output:** A single modified file — `src/News.tsx` — with all design tokens updated to match DESIGN.md, HN-specific branding removed, and existing behaviors preserved.
</objective>

<execution_context>
The executor will implement this plan autonomously. All context needed is provided below — no additional codebase exploration required.
</execution_context>

<context>
@DESIGN.md — Design system tokens and rules (source of truth for all colors, fonts, spacing)
@src/News.tsx — File to be modified (current dark-themed implementation)
@src/Dashboard.tsx — Reference implementation of "Curated Journal" design in a tab screen
@src/Hackathons.tsx — Reference implementation with card patterns and detail overlays
@src/components/PageHeader.tsx — Reusable header component (must use default light mode)
@src/components/DetailOverlay.tsx — Reusable overlay component (already imported in News.tsx)
@src/components/Badge.tsx — Reusable badge/chip component
@src/Heroicon.tsx — Icon component and available icon names

<interfaces>
Key types and contracts the executor needs:

From src/types.ts:
```typescript
export type RootStackParamList = {
  // ...
  News: undefined;
  // ...
};
```

From src/Heroicon.tsx:
```typescript
export type IconName = 'home' | 'menu' | 'user' | 'bell' | 'cog' | 'chevron-left' | 'chevron-right'
  | 'arrow-right' | 'chevron-down' | 'chevron-up' | 'search' | 'x' | 'check' | 'check-circle'
  | 'plus' | 'trash' | 'edit' | 'camera' | 'heart' | 'star' | 'mail' | 'phone' | 'location'
  | 'calendar' | 'clock' | 'globe' | 'link' | 'google' | 'github' | 'terminal' | 'code-bracket'
  | 'book-open' | 'newspaper' | 'book-stack' | 'clipboard-document' | 'briefcase' | 'academic-cap'
  | 'document' | 'shield-lock' | 'lock' | 'eye' | 'eye-slash' | 'bell-slash' | 'shield-check'
  | 'logout' | 'refresh' | 'share' | 'building-library';

export const Heroicon: React.FC<{name: IconName; size?: number; color?: string; strokeWidth?: number}>;
```

From src/components/PageHeader.tsx:
```typescript
type PageHeaderProps = {
  title: string;
  showProfile?: boolean;  // default: true
  showBack?: boolean;
  dark?: boolean;  // use WITHOUT dark prop for light theme
};
```

From src/components/DetailOverlay.tsx:
```typescript
type DetailOverlayProps = {
  visible: boolean;
  onClose: () => void;
  title: string;
  children: React.ReactNode;
  bgColor?: string;  // default: '#ffffff'
};
```

From src/components/Badge.tsx:
```typescript
type BadgeProps = {
  label: string;
  bgColor?: string;  // default: '#d6d4d3'
};
```
</interfaces>

<design_tokens>
These are the exact values from DESIGN.md that the News screen must use:

**Backgrounds (tonal layering):**
- Base/screen: #fcfcfc
- Surface: #f9f9f9
- Surface Container Low: #f3f3f3
- Surface Container: #eeeeee
- Surface Container High: #e2e2e2
- Surface Container Lowest (cards): #ffffff

**Text colors:**
- Primary text (headlines/titles): #1a1c1c
- Secondary text (body): #474747
- Tertiary text (labels, metadata): #5f5e5e
- Muted text (subtle): #9a9a9a / #666666

**Interactive elements:**
- Primary button bg: #1a1c1c (solid) with gradient option (#1a1c1c → #4a4c4c)
- Primary button text: #e2e2e2
- Secondary button bg: #e2e2e2
- Chip/badge bg: #d6d4d3 with border #b0b0b0
- Floating elements: rgba(0,0,0,0.7) for overlays

**Typography:**
- Headlines: fontFamily 'ClashDisplay-Bold', sizes 24-28px
- Subheadings: fontFamily 'CabinetGrotesk-Bold', sizes 16-20px
- Body: fontFamily 'Inter-Regular', size 14-16px
- Labels/UI: fontFamily 'Inter-Medium' / 'Inter-Bold', size 10-13px
- Letter spacing: 1.0-2.0 for all-caps labels

**Card styles:**
- bg: #ffffff
- borderRadius: 20
- No borders (use tonal layering for sectioning)
- Ambient shadow: rgba(0,0,0,0.05), offset (0,4), radius 10, elevation 2

**No-line rule:** Use spacing (32-48px gaps) or background shifts instead of divider lines.
</design_tokens>

<current_issues>
Current News.tsx problems to fix:
1. Background #0d0d0d (dark) → should be #fcfcfc
2. PageHeader used with `dark` prop → should use default light mode
3. HN orange #ff6600 throughout → replace with #1a1c1c
4. "YC" label (HN branding) → remove
5. Domain badge with orange bg (#fef0e5) → remove or restyle in monochrome
6. Fonts use hardcoded names without ClashDisplay/CabinetGrotesk where appropriate
7. Card has visible borderTop/borderBottom (divider, footer border) → use spacing
8. Loading screen uses dark bg → use light bg
9. Detail overlay inline content has orange accents and HN-specific styling
10. Score display uses orange #ff6600 → use monochrome badge
</design_tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme News container, header, loading, and page chrome</name>
  <files>src/News.tsx</files>
  <read_first>
    - src/News.tsx — current file to modify
    - src/Dashboard.tsx — reference for app design token usage
    - src/components/PageHeader.tsx — component API (must NOT use dark prop)
    - DESIGN.md — source of truth for all token values
  </read_first>
  <action>
    Replace all dark-themed container and chrome styling with app's light "Curated Journal" design:

    1. **Container background:** Change `container` style `backgroundColor` from `'#0d0d0d'` to `'#fcfcfc'`

    2. **PageHeader:** Remove the `dark` prop from `<PageHeader title="HN TOP STORIES" dark />`, change to `<PageHeader title="TECH NEWS" />` (new title replacing HN-specific branding). Use default light mode.
       - The title should be "TECH NEWS" (replacing "HN TOP STORIES" per D-07: remove HN branding)

    3. **Loading state:** Replace the dark loading screen:
       - `container` already changed to #fcfcfc
       - Change loading `ActivityIndicator` color from `'#ffffff'` to `'#1a1c1c'`
       - Change loading `Text` color from `'#888'` to `'#5f5e5e'`
       - Change loading text from `"Loading top stories…"` to `"Loading tech news…"`

    4. **Counter/pagination indicator:** Restyle the floating counter at the bottom:
       - Change `counter` style `backgroundColor` from `'rgba(0,0,0,0.7)'` to `'rgba(26,28,28,0.85)'`
       - Change `counterText` color to `'#ffffff'` (keep as is)
       - Padding stays the same

    5. **Card outer container:** Change `cardOuter` paddingHorizontal from 10 to 16 (match app standard spacing)

    6. **Remove HN top bar:** Remove or restyle the `topBar` section — specifically remove the "YC" label and orange score styling:
       - Change `scoreRow` backgroundColor from `'#f5f5f5'` to `'#eeeeee'` (surface-container)
       - Change `scoreNum` color from `'#ff6600'` to `'#1a1c1c'`
       - Change `hnLabel` color from `'#ccc'` to `'#c6c6c6'` (outline-variant) — or remove the YC label entirely by not rendering it. Remove the `<Text style={s.hnLabel}>YC</Text>` line.

    7. **Card background:** Change `card` style `backgroundColor` from `'#ffffff'` to `'#ffffff'` (stays white) — but update shadow to match app pattern:
       - Replace current shadow with: `shadowColor: '#000', shadowOffset: { width: 0, height: 4 }, shadowOpacity: 0.05, shadowRadius: 10, elevation: 2`
       - Keep borderRadius: 24 (slightly larger than app standard 20, but acceptable for featured cards)

    8. **Domain badge:** Replace the orange domain badge with a monochrome version:
       - Change `domainBadge` backgroundColor from `'#fef0e5'` to `'#eeeeee'`
       - Change `domainBadgeText` color from `'#ff6600'` to `'#5f5e5e'`
       - Change fontFamily from `'Inter-Bold'` to `'Inter-Medium'`

    9. **Divider:** Remove the visible divider between title and meta sections. Replace the `divider` style:
       - Change from `{ height: 1, backgroundColor: '#eee', marginVertical: 10 }` to `{ height: 0, marginVertical: 4 }` (spacing instead of line per DESIGN.md no-line rule)

    10. **Title section:** Update `title` typography:
        - Keep fontSize: 24 and fontFamily: 'ClashDisplay-Bold'
        - Keep color: '#1a1c1c'
        - Keep lineHeight: 30

    11. **Meta section:** Keep layout but adjust styling to match app patterns:
        - `avatar` stays same (#1a1c1c bg, white letter)
        - `authorLine` stays same
        - `subLine` stays same

    12. **Read button:** Replace orange read button with app's pill-style button:
        - Change `readBtn` backgroundColor from `'#ff6600'` to `'#1a1c1c'`
        - Keep the layout (flexDirection, padding, borderRadius) same
        - Change `readBtnText` color stays `'#fff'`
        - Update read button text: keep `<Text>READ ON {item.location?.toUpperCase() || 'ARTICLE'}</Text>`

    13. **Detail button:** Keep as typographic link:
        - `detailBtn` and `detailBtnText` stay the same (#999 color, Inter-Bold)

    14. **Footer:** Remove the border-top divider:
        - Change `footer` to remove `borderTopWidth: 1, borderTopColor: '#f0f0f0'`
        - Keep the flexDirection, justifyContent, alignItems, marginTop, paddingTop
    </action>
  <acceptance_criteria>
    1. `grep -c "backgroundColor: '#fcfcfc'" src/News.tsx` returns at least 1
    2. `grep -c "dark" src/News.tsx` returns 0 (no dark prop usage)
    3. `grep -c "#ff6600" src/News.tsx` returns 0 (no orange)
    4. `grep -c "'YC'" src/News.tsx` returns 0 (no YC branding)
    5. `grep -c "fontFamily: 'ClashDisplay-Bold'" src/News.tsx` returns at least 1
    6. `grep -c "borderTopWidth" src/News.tsx` returns 0 (no border dividers)
    7. `grep -c "HN TOP STORIES" src/News.tsx` returns 0 (title changed)
  </acceptance_criteria>
  <verify>
    <automated>node -e "
const fs = require('fs');
const content = fs.readFileSync('src/News.tsx', 'utf8');
const checks = [
  content.includes(\"backgroundColor: '#fcfcfc'\"),
  !content.includes('dark'),
  !content.includes('#ff6600'),
  !content.includes(\"'YC'\"),
  content.includes(\"fontFamily: 'ClashDisplay-Bold'\"),
  !content.includes('borderTopWidth'),
  !content.includes('HN TOP STORIES'),
];
const passed = checks.filter(Boolean).length;
console.log('Passed: ' + passed + '/' + checks.length);
checks.forEach((c, i) => console.log('  Check ' + (i+1) + ': ' + (c ? 'PASS' : 'FAIL'));
process.exit(passed === checks.length ? 0 : 1);
"</automated>
  </verify>
  <done>News.tsx container, header, and chrome use app light theme. No HN orange, no YC branding. PageHeader in default mode. Background is #fcfcfc. Fonts use proper app families.</done>
</task>

<task type="auto">
  <name>Task 2: Re-theme story card content layout (typography, score, meta, footer)</name>
  <files>src/News.tsx</files>
  <read_first>
    - src/News.tsx — current file (post-Task 1 modifications)
    - src/Dashboard.tsx — reference for card typography patterns
    - src/components/Badge.tsx — reusable badge component
    - DESIGN.md — typography and spacing tokens
  </read_first>
  <action>
    Refine card content styling to match app's editorial design language:

    1. **Score badge style:** Restyle the score display to be a monochrome chip:
       - In the `topBar` section, replace the inline score rendering with a cleaner approach:
       - Keep the `scoreRow` as a small badge using app chip styling
       - Change `<Heroicon name="chevron-up" size={12} color="#ff6600" />` to use color `"#1a1c1c"` for the chevron icon (orange already removed in Task 1)
       - Score number: fontSize 13, fontFamily 'Inter-Bold', color '#1a1c1c'

    2. **Domain badge enhancement:** Keep as a subtle monochrome chip (already changed in Task 1):
       - Verify `domainBadge` uses backgroundColor '#eeeeee' and text color '#5f5e5e'
       - Only render domain badge when `item.location` is truthy (already implemented)

    3. **Title refinement:** Ensure headline typography matches editorial style:
       - The card title/headline should use fontFamily 'ClashDisplay-Bold', fontSize 24, color '#1a1c1c', lineHeight 30, letterSpacing -0.3
       - Add `numberOfLines={4}` to title (changed from 5 for cleaner card layout)

    4. **Meta section:** Already matches app pattern — author avatar + name + metadata. Verify:
       - Meta section uses proper spacing between elements
       - Avatar: width/height 34, borderRadius 17, bg #1a1c1c, white letter
       - Author: fontFamily 'Inter-Bold', color '#1a1c1c', fontSize 12
       - Sub-line (comments, category): fontFamily 'Inter-Medium', color '#999', fontSize 10

    5. **Read button refinement:** Style as app's primary pill button:
       - backgroundColor '#1a1c1c', borderRadius 12, paddingVertical 13
       - Text: color '#ffffff', fontSize 12, fontFamily 'Inter-Bold'
       - Icon: Heroicon name "link", size 15, color '#ffffff'
       - Only render when `item.url` is truthy (already implemented)
       - update `activeOpacity={0.85}` on the TouchableOpacity

    6. **Detail button as tertiary link:** Keep as typographic link (already styled correctly):
       - Text: color '#999', fontSize 11, fontFamily 'Inter-Bold'
       - Icon: Heroicon name "arrow-right", color '#999'

    7. **Footer section:** Show rank and date:
       - Rank: fontSize 12, fontFamily 'ClashDisplay-Bold', color '#1a1c1c' (upgraded from Inter-Bold per editorial style)
       - Date: fontSize 9, fontFamily 'Inter-Bold', color '#bbb', letterSpacing 0.4
       - Ensure NO border-top or divider in footer

    8. **Vertical paging behavior:** Preserve the ScrollView with pagingEnabled, snapToInterval, decelerationRate="fast", and onMomentumScrollEnd handler. No changes to scrolling behavior.

    **Preserve ALL of the following without changes:**
    - The `fetchHackerNews()` async function and HN API URL constants
    - The `Story` type definition
    - `extractDomain()`, `formatHNTime()`, `pickCategory()` helper functions
    - `useCardHeight()` hook and card height logic
    - `useEffect` data fetching on mount
    - `onScroll` handler for page tracking
    - All state variables: stories, loading, page, selectedNews, scrollRef
    - Detail overlay open/close logic (selectedNews state management)
    </action>
  <acceptance_criteria>
    1. `grep -n "ClashDisplay-Bold" src/News.tsx | wc -l` is >= 2 (title + rank)
    2. `grep -c "Inter-Medium" src/News.tsx` is >= 2 (sub-line + any other)
    3. `grep -c "noLine" src/News.tsx || grep -c "border" src/News.tsx | xargs -I{} test {} -lt 3` — less than 3 border references
    4. `grep -c "pagingEnabled" src/News.tsx` returns 1 (feature preserved)
    5. `grep -c "fetchHackerNews" src/News.tsx` returns at least 3 (function + call + reference)
    6. `grep -c "HN_TOP_STORIES" src/News.tsx` returns 1 (API URL preserved)
  </acceptance_criteria>
  <verify>
    <automated>node -e "
const fs = require('fs');
const content = fs.readFileSync('src/News.tsx', 'utf8');
const checks = [
  (content.match(/ClashDisplay-Bold/g) || []).length >= 2,
  (content.match(/Inter-Medium/g) || []).length >= 2,
  (content.match(/border/g) || []).length < 3,
  content.includes('pagingEnabled'),
  content.includes('fetchHackerNews'),
  content.includes('HN_TOP_STORIES'),
  content.includes('snapToInterval'),
  !content.includes('#ff6600'),
];
const passed = checks.filter(Boolean).length;
console.log('Passed: ' + passed + '/' + checks.length);
checks.forEach((c, i) => console.log('  Check ' + (i+1) + ': ' + (c ? 'PASS' : 'FAIL')));
process.exit(passed === checks.length ? 0 : 1);
"</automated>
  </verify>
  <done>Card content uses proper editorial typography (ClashDisplay-Bold headlines, Inter body). Score badge is monochrome. No orange accents. Read button styled as app primary button. No border dividers. All HN API logic preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Re-theme DetailOverlay content to match app patterns</name>
  <files>src/News.tsx</files>
  <read_first>
    - src/News.tsx — current file (post-Task 1 & 2)
    - src/components/DetailOverlay.tsx — component API (bgColor prop)
    - src/Hackathons.tsx — reference detail overlay pattern
    - DESIGN.md — design tokens
  </read_first>
  <action>
    Restyle the DetailOverlay content section in News.tsx to match app patterns while keeping the existing DetailOverlay component wrapper:

    1. **DetailOverlay bgColor:** Change the DetailOverlay `bgColor` prop from `"#f9f9f9"` to `"#ffffff"` — use the default DetailOverlay background (per D-06, remove HN theme).

    2. **Category chip:** Replace inline category display with app chip style:
       - Change `ocat` style fontSize to 10, fontFamily to 'Inter-Bold', color to '#5f5e5e', letterSpacing to 1.5
       - Keep marginBottom: 6

    3. **Overlay title:** Update headline typography:
       - `otitle`: fontSize 22, fontFamily 'ClashDisplay-Bold', color '#1a1c1c', lineHeight 26, marginBottom 14

    4. **Divider accent:** Keep the small decorative divider (`odiv`):
       - width 36, height 3, backgroundColor '#1a1c1c', marginBottom 18
       - This is a design accent, not a line-divider (permitted per DESIGN.md)

    5. **Headline/subtitle:** Update sub-headline:
       - `ohead`: fontSize 17, fontFamily 'CabinetGrotesk-Bold', color '#1a1c1c', lineHeight 22, marginBottom 14

    6. **Stats row:** Replace orange stats with monochrome:
       - In `ostatRow`, change each stat icon/text to use '#1a1c1c' color
       - Score: Heroicon name "chevron-up", color '#1a1c1c', text "#1a1c1c"
       - Location: Heroicon name "link", color '#1a1c1c', text "#1a1c1c"
       - `ostatText`: fontSize 12, fontFamily 'Inter-Bold', color '#1a1c1c'

    7. **Content body:** Keep body text style:
       - `obody`: fontSize 13, fontFamily 'Inter-Regular', color '#474747', lineHeight 20

    8. **Open Article button:** Restyle as app primary button:
       - Change `olink` backgroundColor from '#1a1c1c' to '#1a1c1c' (same black - already correct)
       - Ensure borderRadius: 10, paddingVertical: 12
       - Text: color '#ffffff', fontSize 12, fontFamily 'Inter-Bold'
       - Icon: Heroicon name "link", color '#ffffff'
       - Use `activeOpacity={0.85}`

    9. **Overlay footer:** Remove border-top divider, keep spacing:
       - `ofooter`: remove `borderTopWidth: 1, borderTopColor: '#eee'`
       - Keep marginTop: 'auto', paddingTop: 20, flexDirection, justifyContent, spaceBetween
       - `oauthor`: fontSize 11, fontFamily 'Inter-Medium', color '#1a1c1c'
       - `odate`: fontSize 11, fontFamily 'Inter-Regular', color '#9a9a9a'

    10. **Overlay padding:** Keep `overlayPad` style as is (flex: 1, padding: 24)

    **Preserve all of the following:**
    - The conditional rendering: `{selectedNews && (<DetailOverlay ...>...</DetailOverlay>)}`
    - The TouchableOpacity for opening articles with `Linking.openURL`
    - setSelectedNews(null) for closing
    - All selectedNews data access patterns
    </action>
  <acceptance_criteria>
    1. `grep -c "bgColor=\"#ffffff\"" src/News.tsx` returns 1 (or bgColor="#ffffff" or bgColor='#ffffff')
    2. `grep -c "fontFamily: 'ClashDisplay-Bold'" src/News.tsx` includes overlay title
    3. `grep -c "fontFamily: 'CabinetGrotesk-Bold'" src/News.tsx` returns at least 1 (ohead)
    4. `grep -c "fontFamily: 'Inter-Regular'" src/News.tsx` returns at least 1 (obody)
    5. `grep -c "borderTopWidth" src/News.tsx` returns 0
    6. `grep -c "setSelectedNews" src/News.tsx` returns 1 (function reference preserved)
  </acceptance_criteria>
  <verify>
    <automated>node -e "
const fs = require('fs');
const content = fs.readFileSync('src/News.tsx', 'utf8');
const checks = [
  content.match(/bgColor[=:]/) !== null,
  content.includes(\"fontFamily: 'ClashDisplay-Bold'\"),
  content.includes(\"fontFamily: 'CabinetGrotesk-Bold'\"),
  content.includes(\"fontFamily: 'Inter-Regular'\"),
  !content.includes('borderTopWidth'),
  content.includes('setSelectedNews'),
  content.includes('DetailOverlay'),
];
const passed = checks.filter(Boolean).length;
console.log('Passed: ' + passed + '/' + checks.length);
checks.forEach((c, i) => console.log('  Check ' + (i+1) + ': ' + (c ? 'PASS' : 'FAIL')));
process.exit(passed === checks.length ? 0 : 1);
"</automated>
  </verify>
  <done>DetailOverlay content uses app's design tokens. Proper typography hierarchy. Monochrome stats. App-styled article button. No border dividers.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -c "#ff6600" src/News.tsx` returns 0 — no HN orange anywhere
2. `grep -c "#0d0d0d" src/News.tsx` returns 0 — no dark background
3. `grep -c "fontFamily" src/News.tsx` returns >= 6 — proper font tokens throughout
4. `grep -c "ClashDisplay-Bold" src/News.tsx` returns >= 2 — headlines + rank
5. File still has >= 280 lines (logic preserved, only styles changed)
6. `grep -c "HN_TOP_STORIES\|fetchHackerNews\|pagingEnabled\|pickCategory\|extractDomain\|formatHNTime" src/News.tsx` returns >= 6
</verification>

<success_criteria>
- [ ] News.tsx has light background (#fcfcfc) matching other tabs
- [ ] No HN orange (#ff6600) anywhere in the file
- [ ] No YC branding
- [ ] PageHeader uses default (non-dark) mode
- [ ] Typography uses ClashDisplay-Bold, CabinetGrotesk-Bold, Inter-Regular from DESIGN.md
- [ ] Cards use white-on-light tonal layering
- [ ] No border dividers (per no-line rule)
- [ ] DetailOverlay content uses app design tokens
- [ ] All HN API fetching, data processing, vertical paging preserved
- [ ] App still builds: `npx expo export --platform web --output-dir dist 2>&1 | tail -5` (or at minimum TypeScript `npx tsc --noEmit` passes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-news-retheme/01-01-SUMMARY.md` documenting what was changed, design tokens applied, and any deviations from plan.
</output>
